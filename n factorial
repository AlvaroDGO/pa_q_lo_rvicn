#include <bits/stdc++.h>

using namespace std;

int fact(int n);

int main()
{
    int n;
    cin>>n;
    
    cout<<"El factorial es: "<<fact(n);

    return 0;
}

int fact(int n){
    if(n>0){
        return n*fact(n-1);
    } else {
        return 1;
    }
}
